// $(document).ready(() => {
//    role_id = $('#role_id').val();

//    // Phân quyền nhân viên chăm sóc khách hàng
//    $('.admin-link').on('click', function (event) {
//       if (role_id != 2 && role_id != 4) {
//          event.preventDefault(); // Ngăn không cho truy cập vào liên kết
//          Swal.fire("Bạn không có quyền sử dụng chức năng này!");
//       }
//    })

//    // Phân quyền nhân viên kho
//    $('.repo-link').on('click', function(event) {
//       if(role_id != 2 && role_id != 3) {
//          event.preventDefault(); // Ngăn không cho truy cập vào liên kết
//          Swal.fire("Bạn không có quyền sử dụng chức năng này!");
//       }
//    })

//    // Phân quyền nhân viên kế toán
//    $('.accountant-link').on('click', function(event) {
//       if(role_id != 2 && role_id != 1) {
//          event.preventDefault(); // Ngăn không cho truy cập vào liên kết
//          Swal.fire("Bạn không có quyền sử dụng chức năng này!");
//       }
//    })
// })


